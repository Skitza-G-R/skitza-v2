interface:
  display_name: "Verify Skitza"
  short_description: "Run compact code, DB, and browser checks"
  default_prompt: "Use $skitza-verify to run the compact full gate and report VERIFIED, PARTIAL, or the first failure."

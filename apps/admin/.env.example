# Reuse the existing Skitza Clerk application so Gili signs in with her
# normal account. The account must have private metadata:
# { "skitzaAdminRole": "founder" }
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# A random server-only value with at least 32 characters. It signs the
# inactivity token and must differ between deployments.
ADMIN_SESSION_SECRET=

# Server-only bindings. They must identify different Postgres host/database
# targets, not merely differ by credentials or query parameters. Phase 1
# validates but does not connect to them.
ADMIN_LIVE_DATABASE_URL=
ADMIN_TEST_DATABASE_URL=
